<form class="form-horizontal">
    <fieldset>
        <legend>
          <h3>Get started in node.js</h3>
        </legend>
        <p>This is a "get started" code snippet for node.js where you can see how to access your JayStorm service from a node.js application.<br>
        Before you can use this code, you have to install JayData with <i>npm</i> package manager.</p>
        <p><textarea class="console-code" style="font-family:monospace;background:black;color:silver;">npm install jaydata</textarea></p>
        <p>Replace <i>&lt;table name&gt;</i> with your table of choice.</p>
        <textarea class="console-code" id="service-<%=template.name %>-template-<%=service.ServiceID %>" style="width:98%;resize:none;font-family:monospace;background:black;color:silver;"><%
        setTimeout(function(){
            var el = document.getElementById('service-' + template.name + '-template-' + service.ServiceID);
            if (el){
                el.style.height = el.scrollHeight + 'px';
            }
        });
%>require('jaydata');

$data.service('<%=app.url + service.Name %>', function(contextFactory, contextType){
    var <%=service.Name %> = contextFactory();
    <%=service.Name %>.onReady(function(){
        /* <%=service.Name %>.<table name>.toArray(function(result){
            
            ...
            
            <%=service.Name %>.<table name>.add({ ... });
            
            ...
            
            <%=service.Name %>.<table name>.attach(result[0]);
            result[0].<property> = <value>;
            
            ...
            
            <%=service.Name %>.<table name>.remove(result[1]);
            
            ...
            
            <%=service.Name %>.saveChanges(function(result){
            
                ...
                
                console.log(result + ' entity changes processed and saved.');
            
            });
            
        }); */
        
        /* <%=service.Name %>.<table name>.forEach(function(result){
            ...
        }); */
    });
});</textarea>
    </fieldset>
</form>
